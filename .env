# VITE_API_URL is intentionally left unset so the app dynamically uses
# the same hostname as the browser (works for both localhost and phone on WiFi).
# Only uncomment and set this if you have a fixed remote server URL.
# VITE_API_URL=http://localhost:3001
VITE_API_MODE=offline
